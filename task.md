- [x] Execute database migrations for `orders` and `order_items` tables (`supabase/11-orders.sql`)
- [x] Implement Zustand cart store with localStorage persistence (`stores/hooks/use-cart.ts`)
- [x] Connect "Add to Cart" button in product detail page with micro-interactions (`components/index/product-details/product-detail-view.tsx`)
- [x] Update header shopping bag count badge dynamically (`components/index/header.tsx`)
- [x] Refactor cart page to be dynamic with item adjustments and empty state styling (`app/(index)/cart/page.tsx`)
- [x] Update route authorization to allow guest checkouts (`proxy.ts`)
- [x] Implement checkout Server Action with order processing and database insertion (`app/(index)/checkout/actions.ts`)
- [x] Build interactive Checkout form and summary with dynamic Osogbo delivery calculation (`app/(index)/checkout/page.tsx`)
- [x] Connect success screen details and clear cart store on order completion (`app/(index)/order-success/page.tsx`)
- [x] Create database migration for wishlists and reviews (`supabase/12-wishlist-reviews.sql`)
- [x] Expose states and cities via API endpoint `/api/states` (`app/api/states/route.ts`)
- [x] Implement dynamic address dropdowns with `h-10` and `rounded-none` styling (`app/(index)/checkout/checkout-form.tsx`)
- [x] Integrate guest registration option checkbox and password inputs during checkout (`app/(index)/checkout/checkout-form.tsx`, `actions.ts`)
- [x] Implement dynamic Recent Orders query on the user profile (`app/(index)/profile/page.tsx`)
- [x] Implement dynamic Saved Items (Wishlist) query on the user profile (`app/(index)/profile/page.tsx`, `toggleWishlistItemAction`)
- [x] Hook up Heart icon button on product detail views to toggle wishlisted status (`product-detail-view.tsx`, `[slug]/page.tsx`)
- [x] Build `ProfileReviews` client component managing written reviews and "Ready for review" order prompts with star rating selectors (`components/profile/profile-reviews.tsx`, `createProductReviewAction`)
- [x] Split checkout page into separate components under `components/checkout/`
- [x] Create Zustand store `useCheckout` to manage checkout page states (`stores/hooks/use-checkout.ts`)
- [x] Split product detail view into sub-components (`ProductGallery`, `ProductInfo`, `ProductOptions`) under `components/index/product-details/`
- [x] Create Zustand store `useProductDetailStore` to manage product detail page states (`stores/hooks/use-product-detail.ts`)
- [x] Add eye show/hide toggle buttons to checkout password field (`components/checkout/checkout-save-info.tsx`)
- [x] Refactor checkout promo sign-up banner copy to emphasize clothing and fashion accessories (`components/checkout/checkout-promo-banner.tsx`)
